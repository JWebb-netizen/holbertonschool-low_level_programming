#include "main.h"

void print_diagonal(int n)
{
  int i = 0;
  if (n != 0)
    {
     for (i = n; i > 0; i++)
       {
         _putchar(' ');
       }
     _putchar('\\');
    }
  _putchar('\n');
  return;
}
